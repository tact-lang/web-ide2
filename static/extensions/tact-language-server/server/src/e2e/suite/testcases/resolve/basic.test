========================================================================
Simple function resolve
========================================================================
fun add(a: Int, b: Int): Int {
    return a + b;
}

fun test() {
    <caret>add(1, 2);
}
------------------------------------------------------------------------
5:4 -> 0:4 resolved

========================================================================
Local variable resolve
========================================================================
fun test() {
    let value = 1;
    <caret>value + 2;
}
------------------------------------------------------------------------
2:4 -> 1:8 resolved

========================================================================
Local variable resolve before definition
========================================================================
fun test() {
    <caret>value + 2;
    let value = 1;
}
------------------------------------------------------------------------
1:4 unresolved

========================================================================
Local variable resolve with same name function
========================================================================
fun value() {}

fun test() {
    let value = 1;
    <caret>value + 2;
}
------------------------------------------------------------------------
4:4 -> 3:8 resolved

========================================================================
Local variable resolve with same name function in call
========================================================================
fun value() {}

fun test() {
    let value = 1;
    <caret>value();
}
------------------------------------------------------------------------
4:4 -> 0:4 resolved

========================================================================
Parameter resolve
========================================================================
fun test(param: Int) {
    <caret>param + 1;
}
------------------------------------------------------------------------
1:4 -> 0:9 resolved

========================================================================
Extends function resolve
========================================================================
primitive Int;

extends fun increment(self: Int): Int {
    return self + 1;
}

fun test() {
    let num: Int = 1;
    num.<caret>increment();
}
------------------------------------------------------------------------
8:8 -> 2:12 resolved

========================================================================
Unresolved reference
========================================================================
fun test() {
    <caret>unknownVar;
}
------------------------------------------------------------------------
1:4 unresolved

========================================================================
Shadowed variable resolve
========================================================================
fun test() {
    let value = 1;
    {
        let value = 2;
        <caret>value;
    }
}
------------------------------------------------------------------------
4:8 -> 3:12 resolved

========================================================================
Variable in foreach resolve
========================================================================
fun test() {
    foreach (key, value in map) {
        <caret>key;
    }
}
------------------------------------------------------------------------
2:8 -> 1:13 resolved

========================================================================
Variable in foreach resolve 2
========================================================================
fun test() {
    foreach (key, value in map) {
        <caret>value;
    }
}
------------------------------------------------------------------------
2:8 -> 1:18 resolved

========================================================================
Local variable usage in field initializer
========================================================================
struct Foo {
    value: Int;
}

fun test() {
    let value = 1;
    Foo{ value: <caret>value };
}
------------------------------------------------------------------------
6:16 -> 5:8 resolved
