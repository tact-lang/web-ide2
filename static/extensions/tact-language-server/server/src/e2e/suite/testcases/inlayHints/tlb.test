========================================================================
Field TL-B for Int inside map
========================================================================
primitive Int;

contract Foo {
    mp: map<Int, Int>;
}
------------------------------------------------------------------------
primitive Int;

contract Foo {
    mp: map<Int/*  as int257 */, Int/*  as int257 */>;
}

========================================================================
Field TL-B for Int inside map 2
========================================================================
primitive Int;

contract Foo {
    mp: map<Int as int257, Int>;
}
------------------------------------------------------------------------
primitive Int;

contract Foo {
    mp: map<Int as int257, Int/*  as int257 */>;
}

========================================================================
Field TL-B for Int inside map 3
========================================================================
primitive Int;

contract Foo {
    mp: map<Int as int257, Int as int257>;
}
------------------------------------------------------------------------
primitive Int;

contract Foo {
    mp: map<Int as int257, Int as int257>;
}

========================================================================
Field TL-B for Int inside map 4
========================================================================
primitive Int;

contract Foo {
    mp: map<Address, Bool>;
}
------------------------------------------------------------------------
primitive Int;

contract Foo {
    mp: map<Address, Bool>;
}
