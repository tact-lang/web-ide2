========================================================================
initOf with contract with init function
========================================================================
contract MyFoo {
    init(a: Int, b: Int) {}

    fun foo() {
        <caret>initOf MyFoo(10, 20);
    }
}
------------------------------------------------------------------------
4:8 -> 1:4 resolved

========================================================================
initOf with contract with init function inside other function
========================================================================
contract MyFoo {
    init(a: Int, b: Int) {}
}

fun foo() {
    <caret>initOf MyFoo(10, 20);
}
------------------------------------------------------------------------
5:4 -> 1:4 resolved

========================================================================
initOf with contract without init function
========================================================================
contract MyFoo {
    fun foo() {
        <caret>initOf MyFoo(10, 20);
    }
}
------------------------------------------------------------------------
2:8 -> 0:9 resolved

========================================================================
initOf with contract without init function inside other function
========================================================================
contract MyFoo {}

fun foo() {
    <caret>initOf MyFoo(10, 20);
}
------------------------------------------------------------------------
3:4 -> 0:9 resolved
